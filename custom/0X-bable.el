;; See if you can write something like this for erlang
;;(require 'ob-elixir)

(setq org-src-fontify-natively t)

;; example of adding languages to babel
;; (org-babel-do-load-languages
;;  'org-babel-load-languages
;;  '((emacs-lisp . t)
;;    (sh . t)
;; ;;   (elixir . t)
;;    (org . t)
;;    (ditaa . t)))
